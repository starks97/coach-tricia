---
interface Props {
	href: string
	slot?: string
	aria?: string
}

const { href, slot, aria } = Astro.props
---

<a
	slot={slot}
	href={href}
	class="font-tajawal text-taupe mb-2 border-2 px-5 py-2.5 text-center text-xl font-extrabold tracking-[0.07em] uppercase"
	aria-label={aria}><slot /></a
>

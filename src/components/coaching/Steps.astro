---
import data from "../../data.json"

const steps = data.coaching["description"]
---

<div class="section mx-auto w-full space-y-5 px-10">
	<!-- Section Title -->
	<div class="h-auto w-full space-y-5">
		<h1 class="tablet:text-left; text-center font-prata text-5xl text-hero_color tablet:text-6xl">
			Conscious Uncoupling
		</h1>
		<h5
			class="pain-points ml-4 w-full text-center font-tajawal text-xl font-semibold leading-14 tablet:text-2xl"
		>
			5 Steps To Living Happily <span class="font-bold text-hero_color">Even</span> After
		</h5>
	</div>
	<div class="mx-auto w-full max-w-3xl">
		<h5
			class="text-center font-tajawal text-lg font-thin uppercase leading-9 tablet:text-xl tablet:normal-case"
		>
			A powerful 10-Week Course, the 5-Steps of Conscious Uncoupling, offers you the roadmap for how
			to turn the pain of your breakup into a genuine opportunity to recreate your life in a bigger
			and better way. This deeply personal approach will guide you through the devastating storm of
			the loss of love to finding emotional freedom, reclaiming your power and finally to getting
			your life back on track!!
		</h5>
	</div>

	<!-- Steps Grid -->
	<div class="grid h-auto grid-cols-1 justify-items-center">
		{
			steps.map((item) => {
				const isPair = item.id % 2 === 0
				return (
					<div
						class={`flex flex-col max-w-[300px] px-0 tablet:px-10 tablet:max-w-full items-center gap-2 tablet:flex-row ${isPair ? "tablet:flex-row-reverse" : ""}`}
					>
						{/* Text Content */}
						<div class="flex-1 space-y-2 " id={`${item.id}`}>
							<span
								class={`w-full text-center font-bonVivant text-lg text-hero_color2 ${
									isPair ? "tablet:text-center" : "tablet:text-left"
								}`}
							>
								{`Step ${item.id}`}
							</span>

							<h3
								class={`font-prata text-2xl font-semibold ${isPair ? "text-center" : "text-center tablet:text-left"}`}
							>
								{item.title}
							</h3>

							<p class="text-gray-700 text-lg mx-auto mt-5 tablet:max-w-sm text-center font-tajawal tablet:text-left">
								{item.description}
							</p>
						</div>

						{/* Image */}
						<div class="z-10">
						<div class="relative w-[300px] h-[300px] tablet:w-[450px] tablet:h-[450px] bg-[url('/images/tablet.png')] bg-contain bg-no-repeat">
						  <img src={item.image} class="absolute w-[162px] h-[215px] top-[34px] left-[52px] tablet:top-[50px] tablet:right-[0px] tablet:left-[78px] tablet:w-[243px] tablet:h-[325px] object-cover rounded-lg"/>
                        </div>
						</div>

					</div>
					<div
						class="border-black border-[1px] border-hero_color2 mx-auto max-w-5xl w-full translate-y-0 tablet:-translate-y-[80px]"
					/>
				)
			})
		}
	</div>
</div>
